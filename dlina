#include <iostream>
// определение длины строки 
int main() {
    char str[] = "Привет меня зовут алексей как итвои дела";

    int length = 0;
    for (int i = 0; str[i] != '\0'; ++i) {
        length++;
    }

    std::cout << "Длина строки: " << length << std::endl;

    return 0;
}
